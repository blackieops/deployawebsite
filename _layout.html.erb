<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width">
		<link rel="stylesheet" href="/_/css/site.css" type="text/css">
		<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:700,400|Source+Code+Pro:400" type="text/css">
    <title><%= @meta["site_title"] %> <%= @meta["article_title"] %><%= @meta["article_section"] ? ": " + @meta["article_section"] : "" %></title>
	</head>
	<body>
		<div class="container">
			<header class="site-header">
        <h1><a href="/"><%= @meta["site_title"] %></a></h1>
			</header>

      <% if @meta["article_title"] %>
			<header class="site-content-header">
        <h1 class="site-content-title">&hellip; <%= @meta["article_title"] %></h1>
      </header>
      <% end %>

			<article class="site-content">
        <% if @meta["article_section"] %>
        <h1 class="site-content-subtitle"><%= @meta["article_section"] %></h1>
        <% end %>

        <%= @content %>

				<nav class="section-nav">
          <% if @meta["previous_link"] %>
          <div class="section-nav-previous">
            <a href="<%= @meta["previous_link"] %>">&larr; Previous: <%= @meta["previous_text"] %></a>
          </div>
          <% end %>
          <% if @meta["next_link"] %>
          <div class="section-nav-next">
            <a href="<%= @meta["next_link"] %>">Next: <%= @meta["next_text"] %> &rarr;</a>
          </div>
          <% end %>
				</nav>
			</article>

			<footer class="site-footer">
				<div class="legal">
					<p>The content is licensed under the <a href="https://creativecommons.org/licenses/by-nc-sa/4.0/">CC BY-NC-SA 4.0</a> license.</p>
					<p><a href="https://github.com/blackieops/deployawebsite">The code</a> is under the BSD-3 license. Patches welcome!</p>
				</div>

				<div class="credits">
					Presented by:<br>
					<a href="https://www.blackieops.com/"><img src="/_/images/blackieops.svg" alt="BlackieOps" width="100"></a>
				</div>
			</footer>
		</div>
	</body>
</html>
